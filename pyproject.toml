[project]
name = "dwg-rec-system"
version = "0.1.0"
description = "CAD drawing recognition object store and engineering graph foundation."
requires-python = ">=3.10"
readme = "README.md"

[project.scripts]
dwg-rec = "dwg_rec_system.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
